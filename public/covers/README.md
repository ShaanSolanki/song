# Cover art

Drop album covers here, named by each track's `slug`:

```
public/covers/double-take.jpg
public/covers/co2.jpg
public/covers/heather.jpg
...
```

(Square images, ideally ≥ 800×800.) Any missing cover automatically renders a
deterministic gradient placeholder, so the grid never looks broken.
